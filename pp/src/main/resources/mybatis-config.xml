<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- 마이바티스는 sql 구문에 사용되는 값이 NULL 이면 에러 발생함, NULL 을 사용할 수 있게 하려면 -->
	<settings>
		<setting name="jdbcTypeForNull" value="NULL"/>
		<setting name="cacheEnabled" value="false"/>
	</settings>

	<typeAliases>
		<!-- 존재하지 않는 클래스를 등록하면 톰켓 구동 안 됨 -->		
<!-- 		<typeAlias type="org.ict.first.member.model.vo.Member" alias="Member" />
		<typeAlias type="org.ict.first.notice.model.vo.Notice" alias="Notice" />
		<typeAlias type="org.ict.first.board.model.vo.Board" alias="Board" />
		<typeAlias type="org.ict.first.common.Paging" alias="Paging" />
		<typeAlias type="org.ict.first.common.Search" alias="Search" />
		<typeAlias type="org.ict.first.common.SearchDate" alias="SearchDate" />
		<typeAlias type="org.ict.first.test.model.vo.TestVO" alias="TestVO" />
		<typeAlias type="org.ict.first.python.model.vo.Movie" alias="Movie" /> -->
	</typeAliases>
	
	<mappers>
		<!-- 존재하지 않는 파일을 등록하면, 톰켓 구동 안 됨 -->
<!-- 		<mapper resource="mappers/member-mapper.xml" />
		<mapper resource="mappers/notice-mapper.xml" />
		<mapper resource="mappers/board-mapper.xml" />
		<mapper resource="mappers/test-mapper.xml" />
		<mapper resource="mappers/python-mapper.xml" /> -->
	</mappers>
</configuration>








